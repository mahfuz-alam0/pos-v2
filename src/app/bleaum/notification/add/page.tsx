import ComposeNotificationForm from "./ComposeNotificationForm";

export const metadata = { title: "Add Notification" };

export default function AddNotificationPage() {
  return <ComposeNotificationForm />;
}
