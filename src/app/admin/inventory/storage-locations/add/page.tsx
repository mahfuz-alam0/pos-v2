import AddStorageLocationForm from "./AddStorageLocationForm";

export const metadata = { title: "Add Storage Location" };

export default function AddStorageLocationPage() {
  return <AddStorageLocationForm />;
}
